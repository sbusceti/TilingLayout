package it.stefanobusceti.tilinglayout.domain

/** Direction in which a new leaf is inserted relative to an existing target leaf. */
enum class SplitArea {
    Top,
    Bottom,
    Left,
    Right,
}
